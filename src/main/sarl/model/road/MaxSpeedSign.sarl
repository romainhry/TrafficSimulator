package model.road

import model.math.Point2f

class MaxSpeedSign extends Signs {
	var maxSpeed : float
	
	new(x : float, f : float) {
		super(x)
		maxSpeed = f		
	}
	
	def getMaxSpeed : float {
		this.maxSpeed
	}
}
